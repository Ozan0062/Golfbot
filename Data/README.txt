DATA FOLDER

This folder stores images used for testing and calibration.

Possible contents:

* raw_images/
  Images captured from the camera.

* calibration/
  Images used for camera calibration.

* test_images/
  Images used to test detection algorithms without using the camera.

Example usage:
Images from here can be loaded into Python to test object detection
without running the live camera.
