MODELS FOLDER

This folder stores trained object detection models.

Example:

yolov8n.pt

These models are used by the vision system to detect objects
such as the golf ball or obstacles.

The models are loaded during runtime by the image recognition module.
